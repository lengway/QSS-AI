# QSS AI - Детектор подписей, печатей и QR-кодов

AI-система для автоматического обнаружения подписей, печатей и QR-кодов на документах с использованием YOLO11. Поддерживает обработку PDF и изображений с высокой скоростью благодаря параллельной обработке через Ray.

## ✨ Основные возможности

- 🔍 **Детекция объектов**: Автоматическое обнаружение подписей, печатей и QR-кодов
- 📄 **Поддержка PDF**: Обработка многостраничных PDF документов
- 🖼️ **Поддержка изображений**: JPG, PNG форматы
- ⚡ **Высокая производительность**: Параллельная обработка через Ray (4 GPU Actors)
- 🎯 **Точная детекция**: YOLO11n модель, обученная на кастомном датасете
- 📊 **Визуализация**: Интерактивный интерфейс с отображением результатов
- 💾 **Экспорт результатов**: JSON, PDF, изображения
- 🚀 **Batch processing**: Обработка до 100 файлов одновременно

## 🚀 Быстрый старт

### Требования

- **Python 3.8-3.12** (для Ray нужен Python <= 3.12)
- **Node.js 18+** (для frontend)
- **Poppler** (для обработки PDF)
- **CUDA** (опционально, для GPU ускорения)

### Установка Poppler

**Windows:**
1. Скачайте с [GitHub Releases](https://github.com/oschwartz10612/poppler-windows/releases)
2. Распакуйте и добавьте `bin` в PATH

**Linux:**
```bash
sudo apt-get install poppler-utils
```

**macOS:**
```bash
brew install poppler
```

### Backend

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
cd backend
pip install -r requirements.txt

# Настроить переменные окружения (опционально)
cp ../.env.example ../.env
# Отредактируйте .env при необходимости

# Запустить сервер
python main.py
```

Сервер запустится на `http://localhost:8000`

**API документация:** `http://localhost:8000/docs` (Swagger UI)

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend запустится на `http://localhost:3000`

## 📁 Структура проекта

```
innovatex/
├── backend/                    # FastAPI backend
│   ├── main.py                # Основной файл API
│   └── requirements.txt       # Python зависимости
├── frontend/                   # Next.js frontend
│   ├── app/                   # Next.js App Router
│   ├── components/            # React компоненты
│   └── package.json
├── config/                     # Конфигурация YOLO
│   └── data.yaml              # Классы и пути к данным
├── data_newdataset/            # Подготовленный датасет
│   ├── train/                 # Обучающая выборка
│   ├── val/                   # Валидационная выборка
│   └── data.yaml              # Конфигурация датасета
├── runs/                       # Результаты обучения
│   └── detect/
│       └── newdataset_from_scratch/
│           └── weights/
│               └── best.pt    # Обученная модель
├── models/                     # Дополнительные модели
└── .env.example               # Пример конфигурации
```

## 🔧 Конфигурация

Создайте файл `.env` в корне проекта (см. `.env.example`):

```env
# CORS настройки
ALLOWED_ORIGINS=http://localhost:3000

# Путь к модели
MODEL_PATH=./runs/detect/newdataset_from_scratch/weights/best.pt

# Лимиты
MAX_FILE_SIZE_MB=50
MAX_FILES=100

# Параметры детекции
CONFIDENCE_THRESHOLD=0.25
MAX_IMAGE_WIDTH=720
PDF_DPI=100

# Ray настройки (опционально)
USE_RAY=true
RAY_NUM_CPUS=0          # 0 = использовать все CPU
RAY_NUM_ACTORS=0         # 0 = автоматический выбор (4 для GPU)
```

### Параметры конфигурации

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `MODEL_PATH` | Путь к обученной модели | `./runs/detect/newdataset_from_scratch/weights/best.pt` |
| `MAX_FILE_SIZE_MB` | Максимальный размер файла (MB) | `50` |
| `MAX_FILES` | Максимальное количество файлов за раз | `100` |
| `CONFIDENCE_THRESHOLD` | Порог уверенности детекции (0-1) | `0.25` |
| `MAX_IMAGE_WIDTH` | Максимальная ширина изображения (px) | `720` |
| `PDF_DPI` | DPI для конвертации PDF | `100` |
| `USE_RAY` | Использовать Ray для параллелизма | `true` |
| `RAY_NUM_ACTORS` | Количество Ray Actors (0 = авто) | `0` (4 для GPU) |

## 📡 API Endpoints

### `GET /`
Информация о API и версии

### `GET /health`
Health check endpoint

### `POST /api/detect`
Загрузка файлов для детекции

**Параметры:**
- `files`: массив файлов (PDF, JPG, PNG) - до 100 файлов
- `export_json`: boolean (опционально) - экспортировать результаты в JSON
- `include_images`: boolean (опционально, по умолчанию `true`) - включать base64 изображения в ответ

**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/api/detect?export_json=true" \
  -F "files=@document1.pdf" \
  -F "files=@document2.pdf"
```

**Ответ:**
```json
{
  "results": [
    {
      "filename": "document.pdf - Page 1",
      "total": 3,
      "signatures": 1,
      "stamps": 1,
      "qr_codes": 1,
      "detections": [
        {
          "label": "Signature",
          "confidence": 0.95,
          "bbox": {
            "x1": 100.5,
            "y1": 200.3,
            "x2": 250.7,
            "y2": 280.1
          }
        }
      ],
      "image_size": {
        "width": 720,
        "height": 1018
      },
      "image_base64": "data:image/jpeg;base64,..."
    }
  ],
  "summary": {
    "total_files": 1,
    "total_pages": 5,
    "total_detections": 12,
    "signatures": 3,
    "stamps": 2,
    "qr_codes": 7
  },
  "export_data": {...},
  "export_filename": "detections_20251116_120000_dpi100_width720_conf0.25.json"
}
```

**Формат экспорта JSON:**
```json
{
  "filename.pdf": {
    "page_1": {
      "annotations": [
        {
          "annotation_0": {
            "category": "signature",
            "bbox": {
              "x": 1479.04,
              "y": 300.87,
              "width": 226.05,
              "height": 127.8
            },
            "area": 28889.84
          }
        }
      ],
      "page_size": {
        "width": 2480,
        "height": 3509
      }
    }
  }
}
```

## 🎯 Использование

### Через веб-интерфейс

1. Откройте `http://localhost:3000` в браузере
2. Загрузите PDF или изображения (drag & drop или выбор файлов)
3. Нажмите "Analyze Documents"
4. Просмотрите результаты с визуализацией bounding boxes
5. Скачайте результаты в PDF, изображение или JSON

### Через API

См. раздел [API Endpoints](#-api-endpoints) выше.

## ⚡ Производительность

### Оптимизации

- **Ray для параллелизма**: 4 GPU Actors для параллельной обработки
- **Batch processing**: Обработка 4 изображений за раз на каждом Actor
- **GPU ускорение**: FP16 (половинная точность) для 2x ускорения
- **Оптимизированный NMS**: `iou=0.45`, `max_det=300` для быстрой постобработки
- **Условная генерация base64**: Только при необходимости
- **Оптимизированное качество JPEG**: Quality 75 для баланса скорости/качества

### Метрики

- **С GPU (RTX 4060)**: ~10-15 секунд на 50 документов (129 страниц)
- **Без GPU**: Зависит от CPU, но работает
- **Скорость обработки**: ~0.2-0.3 сек на страницу (с GPU)
- **Память**: ~3-4 GB для основного процесса + 0.5 GB на Actor

## 🧪 Обучение модели

### Подготовка датасета

```bash
# Подготовка датасета из PDF с аннотациями
python prepare_newdataset.py
```

### Обучение

```bash
# Обучение модели с нуля на YOLO11n
python train_newdataset.py
```

**Параметры обучения:**
- **Базовая модель**: `yolo11n.pt` (YOLO11 nano)
- **Эпохи**: 100
- **Размер изображения**: 640x640
- **Batch size**: Автоматический (16 для GPU, 4 для CPU)
- **Early stopping**: Patience 40
- **Результаты**: `runs/detect/newdataset_from_scratch/weights/best.pt`

## ⚡ Ray для распараллеливания (опционально)

Ray используется для параллельной обработки страниц PDF через GPU Actors.

**Важно:**
- Ray поддерживает только **Python <= 3.12**
- Если у тебя Python 3.13+ - Ray не установится, но приложение будет работать без него
- Без Ray обработка идет последовательно (тоже работает нормально)

**Установка Ray:**
```bash
pip install 'ray[default]>=2.8.0'
```

**Настройка:**
- Ray включается автоматически если установлен
- Количество Actors: 4 для GPU (оптимально для RTX 4060), автоматически для CPU
- Можно настроить через `RAY_NUM_ACTORS` в `.env`

Если Ray недоступен - приложение автоматически переключится на последовательную обработку.

## 🛠️ Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк
- **YOLO11** (Ultralytics) - детекция объектов
- **PyTorch** - ML фреймворк
- **Ray** - распределенные вычисления (опционально)
- **pdf2image** - конвертация PDF
- **OpenCV** - обработка изображений

### Frontend
- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация

### ML
- **YOLO11n** - базовая модель
- **CUDA** - GPU ускорение
- **FP16** - половинная точность для скорости

## 📊 Что было улучшено

### Производительность
- ✅ Ray для распараллеливания (4 GPU Actors)
- ✅ Batch processing (4 изображения за раз)
- ✅ GPU ускорение с FP16
- ✅ Оптимизированные параметры NMS
- ✅ Параллельная обработка PDF через ThreadPoolExecutor
- ✅ Условная генерация base64
- ✅ Оптимизированное качество JPEG (quality 75)

### Функциональность
- ✅ Поддержка до 100 файлов одновременно
- ✅ Экспорт результатов в JSON (формат all_annotations)
- ✅ Валидация файлов по magic bytes
- ✅ Проверка размера файлов
- ✅ Структурированное логирование (UTF-8)
- ✅ Health check endpoint

### ML/Model
- ✅ Обновлен до YOLO11 (yolo11n.pt)
- ✅ Обучение с нуля на новом датасете
- ✅ Улучшенная детекция (меньше false positives)

## ⚠️ Известные ограничения

- **Ray**: Требует Python <= 3.12
- **Модель**: Должна быть обучена перед запуском
- **Память**: При обработке большого количества файлов может потребоваться много RAM
- **GPU**: Рекомендуется для лучшей производительности

## 📝 Лицензия

Проект создан для хакатона.

## 👥 Авторы

@lengway

## 🔗 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Ultralytics YOLO](https://docs.ultralytics.com/)
- [Ray документация](https://docs.ray.io/)
- [Next.js документация](https://nextjs.org/docs)
